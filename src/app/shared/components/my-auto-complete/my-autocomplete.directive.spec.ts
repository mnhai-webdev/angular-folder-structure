import { MyAutocompleteDirective } from './my-autocomplete.directive';

describe('MyAutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new MyAutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
